@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Confirm email";
    ViewData["Subtitle"] = "Account activation status.";
}

<partial name="_PageHeader" />

<div class="card mb-3">
    <div class="card-body">
        <partial name="_StatusMessage" model="Model.StatusMessage" />
        <div class="mt-3">
            <a class="btn btn-outline-secondary" asp-page="./Login">Back to login</a>
        </div>
    </div>
</div>
