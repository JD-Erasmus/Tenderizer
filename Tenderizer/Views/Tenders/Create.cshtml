@using Tenderizer.ViewModels
@model TenderUpsertVm

@{
    ViewData["Title"] = "Create Tender";
    ViewData["Subtitle"] = "Capture tender details and upload the request document if available.";
    ViewData["ShowTenderRequestUpload"] = true;
}

<partial name="_PageHeader" />

<div class="card mb-3">
    <div class="card-body">
        <form method="post" enctype="multipart/form-data">
            @Html.AntiForgeryToken()
            <partial name="_TenderForm" model="Model" />

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">Create</button>
                <a class="btn btn-outline-secondary" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
